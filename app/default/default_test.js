'use strict';

describe('mdtcrm.default module', function() {

  beforeEach(module('mdtcrm.default'));

  describe('default controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var defaultCtrl = $controller('defaultCtrl');
      expect(defaultCtrl).toBeDefined();
    }));

  });
});