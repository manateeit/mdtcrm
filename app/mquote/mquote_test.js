'use strict';

describe('mdtcrm.mquote module', function() {

  beforeEach(module('mdtcrm.mquote'));

  describe('mquote controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mquoteCtrl = $controller('mquoteCtrl');
      expect(mquoteCtrl).toBeDefined();
    }));

  });
});