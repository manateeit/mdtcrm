'use strict';

describe('mdtcrm.mspec module', function() {

  beforeEach(module('mdtcrm.mspec'));

  describe('mspec controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mspecCtrl = $controller('mspecCtrl');
      expect(mspecCtrl).toBeDefined();
    }));

  });
});